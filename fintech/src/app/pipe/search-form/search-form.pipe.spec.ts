import { SearchFormPipe } from './search-form.pipe';

describe('SearchFormPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFormPipe();
    expect(pipe).toBeTruthy();
  });
});
